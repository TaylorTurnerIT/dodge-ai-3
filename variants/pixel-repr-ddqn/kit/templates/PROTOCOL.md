# Execution protocol template

Template only. Copy and fill before collection/training. Unresolved fields block launch; this file authorizes nothing. SPEC §E owns maxima; duplicate no independent limits here.

- Phase / task IDs / accepted predecessors:
- Requested execution scope / authorization reference:
- SPEC revision / source commit / accounted-for dirty diff hash:
- Variant / architecture ID / exact command after implemented:
- Config path and hash / dependency lock hash / runtime versions:
- Device / memory ceiling / storage ceiling / device-hour ceiling:
- Dataset IDs/hashes / episode split manifest / game-seed pools:
- Learner seeds / screen versus confirmation identity:
- Input/target whitelist / native decision cadence / boundary masks:
- Training conditions / baseline controls / fixed-reference encoder/readout hashes:
- Updates/transitions per condition and seed / total all-condition cost:
- Shared-corpus exposure accounting / offline-RL updates / online schedule:
- Evaluation transitions/cap / checkpoint selection rule / censoring policy:
- Metrics / exact numeric gates / collapse control calibration reference:
- Scene review clip IDs / review criteria / allowed validation partition:
- Paired uncertainty method / resampling unit / confidence level:
- Holdout sealed state and opening rule:
- Output root / run IDs / checkpoint and interruption policy:
- Accepted regression evidence / preflight hash verification:
- Who accepted protocol / when / next action:
